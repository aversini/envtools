#!/usr/bin/env node

/**
 * This is the internal Envtools API documentation. It is not intended to be
 * used by Envtools users, but rather by Envtools developers.
 * It is an easily accessible repository of all the methods
 * available in one centralized document (automatically generated
 * via yuidoc).
 *
 * @module envtools
 */

require('../lib/cli/cli').parseCommandLine();
