'use strict';const _=require('lodash/core'),moment=require('moment'),path=require('path'),fs=require('fs-extra'),execa=require('execa'),BACKUPDIR=path.join(process.env.HOME,'.envtools','backups');module.exports=(a)=>{let b;const c=path.join(BACKUPDIR,moment().format('MMDDYYYY-HH[h]mm[m]'));fs.ensureDirSync(c),b=_.isArray(a)?a.join('" "'):a;try{execa.shellSync(`cp -Rf "${b}"  "${c}"`)}catch(a){}return c};